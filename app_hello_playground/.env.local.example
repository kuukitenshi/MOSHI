# Hellō quickstart baseline app (RQ1 comparison) — local config.
#
# Copy this to `.env.local` (git-ignored, never commit) before running the app.
# The cookie secret is generated for you by the Hellō quickstart; you can also
# set your own 32-byte hex value.
HELLO_COOKIE_SECRET=
